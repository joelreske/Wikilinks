var PlayGame = React.createClass({
    render: function () {
        return (
            <div className="game"> 
            //start and render clock
            //render back button
            //render stop count
            //back button should decrement stop count
            //clicking should increment count 
            //current artical name is rendered under that
            //buttons in between of all the places
            //Desination is rendered at bottom 

            </div>

        );
    }
});